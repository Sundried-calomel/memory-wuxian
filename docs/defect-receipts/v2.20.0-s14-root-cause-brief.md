# Defect Prevention Packet

- Task: `memory-wuxian-v2.20.0-s14-root-cause-and-lean-recovery`
- Packet: `packet:f152957f6001da407dd3a6a4`
- Status: `ready`
- Blocking limit: 12
- Projection limit: 2500 estimated tokens

## G01 环境存在不等于能力可用

- Class: `blocking`; score: 123; severity: `high`
- Guidance: Resolve the declared runtime and prove the exact capability, imports, permissions, version, and working directory; never infer readiness from PATH or process existence.
- Forbidden: Treating command existence or process existence as sufficient proof.; Installing a global dependency before checking a registered project runtime.
- Required: Resolve an explicit runtime or tool registry when available.; Probe version, protocol, critical imports, permissions, and working directory.
- Checks: runtime-capability-probe (L2): Execute the exact required capability with the selected runtime and working directory.

## G03 修复入口没有覆盖后来新增的入口

- Class: `blocking`; score: 78; severity: `high`
- Guidance: Inventory every entrypoint touching the resource and reuse one implementation or one contract suite; a new UI, updater, or worker must inherit prior regressions.
- Forbidden: Copying a previously fixed boundary implementation into a new entrypoint.
- Required: Enumerate all entrypoints and their Owner before implementation.; Route through a shared implementation or shared contract tests.
- Checks: entrypoint-inventory (L0): List every entrypoint that reaches the affected resource.; cross-entrypoint-contract (L2): Run the same historical regression cases through every affected entrypoint.

## G04 默认编码与字符串命令破坏特殊字符

- Class: `blocking`; score: 120; severity: `high`
- Guidance: Use UTF-8 and structured argument or parser boundaries; test multilingual, currency, emoji, spaces, leading hyphens, and long paths.
- Forbidden: Concatenating untrusted or path-bearing values into a shell command string.; Using fuzzy path matches for irreversible or authoritative selection.
- Required: Use argument arrays and structured parsers.; Test Chinese, Japanese, currency symbols, emoji, spaces, leading hyphens, and long paths.
- Checks: unicode-path-fixtures (L2): Round-trip multilingual and special-character paths and values through the production boundary.

## G05 模拟测试替代真实平台效果

- Class: `blocking`; score: 113; severity: `high`
- Guidance: Mocks and schemas cannot prove UI, animation, installation, or background effects; run at least one real target-platform case.
- Forbidden: Using a mock, file count, schema pass, or process launch as the sole proof of platform behavior.
- Required: Validate UI visually and interactively, background work by state transition, and installers by upgrade and rollback.
- Checks: target-platform-effect (L3): Exercise the production platform path and capture its visible or persisted result.

## Authorization Boundaries

- Raw archives, summaries, cloud exchange, Environment state, and user data are outside this repair.
- The current failed candidate, S14 logs, historical receipts, and rollback evidence remain immutable evidence; historical records may be archived out of the production package but not rewritten or discarded.
- No production edit or broad replan is allowed before the exact broker/controller failure is proved from the frozen artifact path.
- Deletion requires reachability, owner, package-membership, and behavior-equivalence evidence for each fixed candidate path.
- No S14 install or S15 release may occur until one exact packaged production chain rehearsal passes and is bound to the candidate SHA-256.

## Invalidation

- The task manifest bytes or declared scope change.
- A catalog, source workbook binding, rule status, or threshold changes.
- The implementation plan adds an undeclared unit, owner, path, entrypoint, resource, or operation.

Packet SHA-256: `c5beaf6166bc273bab4e082ba50bb100cd7eb0f4893cadeac3da6afe5659dd97`
