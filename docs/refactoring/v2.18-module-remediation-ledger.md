# Memory 无限 v2.18 模块整改台账

## 1. 口径

本台账补齐 V0 时承诺但此前没有形成正式文件的交付物：每个模块当前在哪里、发现了什么、已经怎样修、下一步怎样修、预计影响多少行、用什么证据验收。

- V0 基线：`72998fd4e051816f57ac2c56d94579e0db45f8b1`
- V1 架构门禁：`c4ae78dabae8c0f2618288d6e6a5957af07cf6a7`
- V2 当前状态：`6c514ccbdd075a5f45cf1d956471177edf7555ee`
- 生产范围：`scripts/**/*.py|ps1|sh`、`native-collector/src/**/*.rs`、`packaging/**`
- 计数方法：读取 Git 对象和当前文件，按物理行计数；空行也计入。

状态定义：

- **已完成/实测**：代码已经落地，行数和验证结果可从提交与收据复算。
- **待实施/估算**：策略已确定，但代码尚未修改；行数只能给范围或明确不估。
- **延期**：当前明确不动，避免触碰尚未稳定的摘要合同。

## 2. 总体结果

| 指标 | V0 | V3 完成 | 变化 | 结论 |
|---|---:|---:|---:|---|
| 生产文件 | 96 | 96 | 0 | 没有通过拆出大量小文件伪造精简 |
| 生产范围物理行 | 46,403 | 46,778 | +375 | V1 增加架构门禁；V2 净减 40 行；V3 的 Rust 文件增加 89 行生产区和 255 行内嵌测试 |

`+375` 不是“重构失败”，也不能写成“精简成功”。V3 的核心入口明显缩短，但用显式类型、共享状态转换和回归测试替代了动态 JSON 与重复控制流；行数只是辅助指标。

## 3. 已完成整改

| 阶段/模块 | V0 问题 | 已完成修复与当前入口 | 实际行数变化 | 已有验证 |
|---|---|---|---:|---|
| V1 Product Quality 架构门禁 | Rust 禁止能力、打包所有权和 Python 依赖边界只存在于描述中，无法阻止回归 | 扩展 `docs/module-architecture.json:1-237`；由 `scripts/check_architecture_contract.py:1-312` 执行；`tests/test_product_architecture.py:1-278` 固化反例 | 611 → 827，`+216` | 架构检查通过；39 项定向架构及发布合同测试通过 |
| V2 Platform Foundation | 命令参数去重和 active-root 指针在多个调用方重复实现 | 统一到 `scripts/platform_process.py:10-16` 的 `_unique_command_argument` 与 `scripts/platform_paths.py:11-17` 的 `active_root_pointer` | 相关六文件 9,142 → 9,149，`+7` | 286 项 Python 影响测试通过，16 项平台跳过；Unicode 路径夹具通过 |
| V2 Capture WAL | WAL 与事务层各自维护追加写入细节，父目录缺失行为可能漂移 | 统一到 `native-collector/src/store/transaction.rs:50-66` 的 `append_bytes_prepared`，`native-collector/src/store/wal.rs:131-143` 复用 | 367 → 396，`+29` | 33 项 Rust 测试通过，覆盖精确 WAL 字节和父目录边界 |
| V2 Environment | 严格键检查及相邻解析逻辑在 bindings/conflicts/incoming/promotions/skills 重复 | 统一到 `scripts/memory_environment.py:100-111` 的 `_strict_keys`，调用方删除重复实现 | 5,332 → 5,256，`-76` | Environment 完整影响集通过；字节、哈希和错误顺序保持 |
| V3a Capture 类型化批处理 | `sync_batch_mode` 通过动态 `Value` 字段累加和覆盖，字段漂移只能在运行时发现 | `native-collector/src/lib.rs:486-642` 定义类型链；`2750-2838` 仅在最终边界恢复原 JSON | 主函数 168 → 89，`-79`；类型与转换新增 139 行 | 完整、partial、跨批次合并黄金测试通过；冻结旧二进制与候选在普通及 521 条多批次输入下 stdout 逐字节一致；独立审计 R3 PASS |
| V3b Capture 共享事件周期 | macOS 与非 macOS 重复维护发现、同步、coverage、telemetry 和 stamp 推进顺序 | `native-collector/src/lib.rs:3448-3543` 统一状态转换；平台 watcher 保留在 `3546-3639` | 两套循环 137 → 92，`-45`；共享状态与转换 96 行；该区域净增 51 行 | 成功同步后推进 stamp、失败不推进的生产路径测试通过；macOS ARM 分支交叉编译通过；独立审计 R3 PASS，未宣称 Mac 真机运行 |

V2 各已改文件的实测明细：

| 文件 | V0 | V2 | 差值 |
|---|---:|---:|---:|
| `scripts/collector_lifecycle.py` | 552 | 550 | -2 |
| `scripts/install_macos_transaction.py` | 1,435 | 1,437 | +2 |
| `scripts/install_codex_autosync_windows.py` | 891 | 888 | -3 |
| `scripts/memory_cli.py` | 6,214 | 6,210 | -4 |
| `scripts/platform_paths.py` | 21 | 28 | +7 |
| `scripts/platform_process.py` | 29 | 36 | +7 |
| `native-collector/src/store/transaction.rs` | 57 | 78 | +21 |
| `native-collector/src/store/wal.rs` | 310 | 318 | +8 |
| `scripts/memory_environment.py` | 1,020 | 1,021 | +1 |
| `scripts/memory_environment_bindings.py` | 1,181 | 1,160 | -21 |
| `scripts/memory_environment_conflicts.py` | 385 | 377 | -8 |
| `scripts/memory_environment_incoming.py` | 618 | 601 | -17 |
| `scripts/memory_environment_promotions.py` | 437 | 427 | -10 |
| `scripts/memory_environment_skills.py` | 1,691 | 1,670 | -21 |

## 4. 待实施整改

| 阶段/模块 | 当前代码位置 | 具体实施路径 | 预计行数影响 | 风险与验收门槛 |
|---|---|---|---|---|
| V3 后续 Capture 修复编排 | `native-collector/src/lib.rs:2843-2920`，78 行 | 将 recovery-debt 编排移到 Control/Application 边界；Capture Core 只暴露稳定原语和类型化结果 | 暂不估算 | marker、恢复顺序、失败收据和幂等性必须建立黄金夹具；未单独冻结合同前不实施 |
| 后续 CLI 命令注册表 | `scripts/memory_cli.py:4244-5059`、`5103-6079`、`6082-6206`，合计 1,918 行 | 按 owner 小批迁移到唯一不可变命令注册表，由同一记录生成 parser 和 dispatch；不一次性重写全部 CLI | 暂不估算，初期很可能增加 | 先生成完整命令清单和锁定策略夹具；逐 owner 迁移，每批验证 help、参数、退出码和输出 |
| V4a Capture 消息缓存 | `native-collector/src/lib.rs:1169-1355`、`1395-1446` | 使用紧凑 ID 索引与 O(1) 最大 sequence；只有重复修复时才惰性加载完整记录 | 预计增加代码，不以减行为目标 | 基准必须先证明冷启动、追加延迟和 RSS 改善；原始记录字节及 sequence 权威不变 |
| V4b Rollout SourceSnapshot | `native-collector/src/lib.rs:3176-3217`，由共享事件周期调用 | 单次枚举形成不可变快照，复用于 stamp、cursor、coverage 和事件周期 | 暂不估算 | 先记录枚举次数、目录读取量和时延；证明无漏采、无重复采集后才能替换 |
| V4c 状态台 BuildContext | `scripts/memory_dashboard.py:870-1050`、`1171-1249`；`scripts/memory_cli.py:3848-3899` | 建立一次性的 raw/summaries/ledgers 快照，在同一刷新周期内共享；缓存只保存明确版本化的投影 | 预计 `-30` 至 `+40`，仅为估算 | 先建冷刷新/增量刷新夹具；验证显示值、待处理数、隔离数、缓存失效和 Unicode 标题 |

## 5. 明确延期

| 模块 | 当前位置 | 延期理由 | 重新启动条件 |
|---|---|---|---|
| 确定性索引刷新 | `native-collector/src/lib.rs:2246-2443`，198 行 | 与摘要/索引合同耦合，当前不应在结构梳理中顺手改动 | 新摘要模块完成独立验收，冻结索引输入输出合同 |
| 一级摘要任务创建 | `native-collector/src/lib.rs:2444` 起 | 用户已明确要求等待新的原子化摘要规则跑通并完成历史回填 | 新模块有稳定版本、迁移策略、回滚路径和真实积压彩排 |
| MemoryStore 大拆分 | `scripts/memory_cli.py` | 直接大拆会同时触碰归档、摘要、检索、状态和兼容入口 | 先完成 V3/V4，并按 owner 逐段迁移，不做一次性重写 |

## 6. 实施顺序

1. V3a 与 V3b 已在 `91a7cb3` 完成候选冻结，并以独立审计 R3 `PASS`（99.15/100）关闭 V3。
2. V4 先提交性能夹具，不先提交优化；随后一次只改消息缓存、SourceSnapshot 或 BuildContext 中的一项。
3. 每个小补丁只跑影响范围彩排；最终候选版本才跑一次全量彩排。不得为发现一个小问题连续发布多个补丁版。
4. 摘要与确定性索引继续冻结，等新摘要模块满足重启条件后作为独立阶段处理。

## 7. 结论

V1、V2 已完成并有独立提交；V3a/V3b 已完成实现、彩排和独立审计，并正式关闭；V4 仍未实施。本台账是该轮结构梳理的唯一进度入口，不能再用聊天中的零散结论代替。
