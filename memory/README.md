# Memory無限 Archive

Raw history is authoritative. Summaries and indexes provide retrieval routes.

Use `heartbeat --check-only` for a read-only integrity check. Preview `rebuild-state` or `rebuild-indexes` before applying recovery. Reconstruction may replace derived files after archiving them, but it never edits raw messages or summary files.
